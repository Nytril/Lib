include "Help";

using Format;

Range = -Math.PI2..Math.PI2;

Description = DescriptionBlock {
  EquationPar {
    var x = Symbol("x");
    Symbol("y");
    Tex.Equals;
    Fraction {
      Symbolic.E sup x - Symbolic.E sup -x;
      Symbolic.E sup x + Symbolic.E sup -x;
    };
  };

  Chart(100%, 2") {
    ChartType: ChartTypes.Line;
    XAxis: ChartAxis {
      TextDigits: 1;
      Range step Math.PI/6;
    };
    ValueAxis: ChartAxis;

    ChartSeries {
      foreach (var x in Range step Math.PI/16)
        new DataPoint(x, Math.Tanh(x));
    };
  };
};

ShowTanh(x) = Span {
  TextDigits: 8;
  "The hyperbolic tangent of {0} is {1}."(x, Math.Tanh(x));
};

Example = Block {
  ShowTanh(0);
  ShowTanh(Math.PI / 3);
  ShowTanh(Math.PI / 2);
};

Main.Help = new HelpExample(attribute(Math.Tanh));


